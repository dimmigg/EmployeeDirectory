<UserControl x:Class="EmployeeDirectory.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmployeeDirectory.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl>
        <TabItem Header="Ведомость по зарплате">
            <Frame Content="{Binding SalaryReport}"
               VerticalAlignment="Top"
               NavigationUIVisibility="Hidden"
               Width="700"
               Height="360"/>
        </TabItem>
        <TabItem Header="Список сотрудников">
            <Frame Content="{Binding EmployeesList}"
               VerticalAlignment="Top"
               NavigationUIVisibility="Hidden"
               Width="700"
               Height="360"/>
        </TabItem>
    </TabControl>
</UserControl>
